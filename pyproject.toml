[project]
name = "agent-diagnosis"
version = "0.1.0"
description = "Active Diagnosis for Failed AI-Agent Execution Traces"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]
llm = ["anthropic>=0.40"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
